# Edit with your values and rename to serverless.env.yml
dev:
  CUSTOM_DOMAIN: 'test.my-domain.com'
  BUCKET_NAME: 'my-test-bucket-name'
  ACM_CERT_ARN: 'get the arn from the cert manager'
prod:
  CUSTOM_DOMAIN: 'my-domain.com'
  BUCKET_NAME: 'my-bucket-name'
  ACM_CERT_ARN: 'get the arn from the cert manager - for the TLD'
HOSTED_ZONE_NAME: 'my-domain.com.' # <- the dot is intentional, a route53 requirement
