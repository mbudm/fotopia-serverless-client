language: node_js
node_js:
- node
cache:
  yarn: true
  directories:
    - node_modules
before_script:
  - sh serverless.env.sh
script:
  - yarn lint
  - yarn test-once
  - yarn build
  - sls deploy -s dev
  - sls deploy -s prod
